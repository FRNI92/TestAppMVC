@{
    Layout = "_PortalLayout";//makes it use the correct layout to include menu and settings
    ViewData["Title"] = "Clients";
}

<partial name="Partials/Sections/_AddClientForm" />
<partial name="Partials/Sections/_EdithClientForm" />
<section id="clients">
    <header>
        <h2>Clients</h2>
        <button type="button" data-modal="true" data-target="#addClientModal">AddClient</button>
    </header>

    <div>
        @await Html.PartialAsync("Partials/Sections/_ClientList") 
    </div>
    @* important! use await!!!! *@
</section>


